{
  "name": "collective.js.throttledebounce",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "git@github.com:collective/collective.js.throttledebounce.git",
  "author": "yarn add -D requirejs chokidar-cli",
  "license": "GPL2",
  "private": true,
  "scripts": {
    "build": "r.js -o build.js",
    "build-js": "r.js -o build.js",
    "watch": "chokidar \"./collective/js/throttledebounce/resources/(*.js|*.css)\" -i \"**/*-compiled.(js|css)\" -c \"/bin/sh -c \\\"if [[ '{path}' = *.js ]]; then r.js -o build.js; else r.js -o buildcss.js; fi;\\\"\""
  },
  "devDependencies": {
    "chokidar-cli": "^2.1.0",
    "requirejs": "^2.3.6"
  }
}
